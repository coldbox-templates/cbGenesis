/**
 * The base model test case will use the 'model' annotation as the instantiation path
 * and then create it, prepare it for mocking and then place it in the variables scope as 'model'. It is your
 * responsibility to update the model annotation instantiation path and init your model.
 */
class extends="tests.resources.BaseIntegrationSpec" {

	@inject
	property name="settingService";

	/*********************************** BDD SUITES ***********************************/

	function run() {
		describe( "A Setting", () => {
			beforeEach( () => {
				variables.setting = settingService.new()
			} )

			it( "can be created", () => {
				expect( setting ).notToBeNull()
			} )

			it( "initializes with default values", () => {
				expect( setting.isLoaded() ).toBeFalse()
				expect( setting.getName() ).toBe( "" )
				expect( setting.getValue() ).toBe( "" )
				expect( setting.getDescription() ).toBe( "" )
			} )


		} )
	}
}