<!--- Sidebar Footer Component
	Renders the user profile link in the cb-sidebar footer. Uses prc.authUser data

	Usage:
		#view( "_components/app/sidebar-footer" )#

	Reads from prc (populated by protected handlers):
		prc.authUser.initials — user avatar letter
		prc.authUser.name     — display name
		prc.authUser.role     — role label --->
<bx:output>
<div class="cb-sidebar-footer" id="cb-sidebar-footer">

	<!--- User profile link --->
	<a
		href="#event.route( "profile" )#"
		class="cb-sidebar-user text-decoration-none"
				@click="$store.sidebar.close()">

		<!--- User Avatar --->
		#view( view: "_components/ui/avatar", args: {
			userId   : prc.authUser.getUserId(),
			hasAvatar: prc.authUser.getHasAvatar(),
			initials : ( prc.authUser.getInitials() ?: "A" ),
			class    : "cb-sidebar-user-avatar",
			alt      : ( prc.authUser.getFullName() ?: "Admin User" )
		} )#

		<!--- User Info --->
		<span class="cb-sidebar-user-info">
			<span class="cb-sidebar-user-name">
				#( prc.authUser.getFullName() ?: "Admin User" )#
			</span>
		</span>
	</a>

</div>
</bx:output>