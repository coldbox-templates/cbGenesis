/**
 * Seeds the database with the initial admin role, permissions, and admin user.
 *
 * Run with: migrate seed run
 */
class {

	static {
		// bcrypt of the well-known bootstrap password "test". This hash is public: it
		// ships in the repository, so it must never be a password anyone keeps. The
		// admin below is therefore seeded with isPasswordReset = true, which forces a
		// new password on first sign-in before the account can be used.
		test_bcrypt = "$2a$10$nIfWUb9y5BtKWWNinHxtkOo9QIg.AaoVh.7MbukPrg5uLX9BNVC42"
	}

	function run( qb, mockdata ) {

		// Postgres's uuid/json columns are natively typed and its JDBC driver refuses to
		// implicitly cast a bound varchar parameter to them ("column is of type X but
		// expression is of type character varying"). Binding as sqltype "other" fixes that
		// on Postgres, but MySQL/MSSQL's drivers reject "other" outright for plain string
		// values, so only apply it on Postgres - everywhere else the columns are looser
		// (CHAR(36)/UNIQUEIDENTIFIER, TEXT) and a plain string binds just fine.
		var isPostgres = getSystemSetting( "DB_DRIVER", "mysql" ) == "postgresql"
		var pgTyped    = ( value ) => isPostgres ? { value: value, sqltype: "other" } : value

		var adminRoleId = "00000000-0000-0000-0000-000000000001"
		var permissions = [
			{
				id          : "00000000-0000-0000-0000-000000000101",
				permission  : "roles:read",
				description : "View roles"
			},
			{
				id          : "00000000-0000-0000-0000-000000000102",
				permission  : "roles:write",
				description : "Create and update roles"
			},
			{
				id          : "00000000-0000-0000-0000-000000000103",
				permission  : "roles:delete",
				description : "Delete roles"
			},
			{
				id          : "00000000-0000-0000-0000-000000000104",
				permission  : "roles:admin",
				description : "Full role administration"
			},

			{
				id          : "00000000-0000-0000-0000-000000000201",
				permission  : "users:read",
				description : "View users"
			},
			{
				id          : "00000000-0000-0000-0000-000000000202",
				permission  : "users:write",
				description : "Create and update users"
			},
			{
				id          : "00000000-0000-0000-0000-000000000203",
				permission  : "users:delete",
				description : "Delete users"
			},
			{
				id          : "00000000-0000-0000-0000-000000000204",
				permission  : "users:admin",
				description : "Full user administration"
			},

			{
				id          : "00000000-0000-0000-0000-000000000301",
				permission  : "permissions:read",
				description : "View permissions"
			},
			{
				id          : "00000000-0000-0000-0000-000000000302",
				permission  : "permissions:write",
				description : "Create and update permissions"
			},
			{
				id          : "00000000-0000-0000-0000-000000000303",
				permission  : "permissions:delete",
				description : "Delete permissions"
			},
			{
				id          : "00000000-0000-0000-0000-000000000304",
				permission  : "permissions:admin",
				description : "Full permission administration"
			},

			{
				id          : "00000000-0000-0000-0000-000000000401",
				permission  : "settings:read",
				description : "Read settings"
			},
			{
				id          : "00000000-0000-0000-0000-000000000402",
				permission  : "settings:write",
				description : "Create and update settings"
			},
			{
				id          : "00000000-0000-0000-0000-000000000403",
				permission  : "settings:delete",
				description : "Delete settings"
			},
			{
				id          : "00000000-0000-0000-0000-000000000404",
				permission  : "settings:admin",
				description : "Full settings administration"
			},

			{
				id          : "00000000-0000-0000-0000-000000000501",
				permission  : "auditlog:read",
				description : "View audit log entries"
			},
			{
				id          : "00000000-0000-0000-0000-000000000502",
				permission  : "auditlog:export",
				description : "Export audit log entries"
			},
			{
				id          : "00000000-0000-0000-0000-000000000503",
				permission  : "auditlog:delete",
				description : "Purge audit log entries by age"
			},
			{
				id          : "00000000-0000-0000-0000-000000000504",
				permission  : "auditlog:admin",
				description : "Full audit log administration, including clearing all entries"
			}
		]

		// Create Admin role
		qb.newQuery().from( "roles" ).insert( {
			roleId      : pgTyped( adminRoleId ),
			role        : "Admin",
			description : "System Administrator",
			isActive    : true
		} )

		// Create permissions and assign all to Admin
		permissions.each( ( permission ) => {

			qb.newquery().from( "permissions" ).insert( {
				permissionId : pgTyped( permission.id ),
				permission   : permission.permission,
				description  : permission.description,
				isActive     : true
			} )

			qb.newquery().from( "role_permissions" ).insert( {
				FK_roleId       : pgTyped( adminRoleId ),
				FK_permissionId : pgTyped( permission.id )
			} )

		} )

		// Create Admin user and assign Admin role
		var adminUserId = "00000000-0000-0000-0000-000000000001"
		qb.newQuery().from( "users" ).insert( {
			userId    : pgTyped( adminUserId ),
			isActive  : true,
			firstName : "Admin",
			lastName  : "User",
			email     : "admin@cbgenesis.com",
			password  : static.test_bcrypt,
			biography : "I am a wonderful CBGenesis Admin",
			preferences: pgTyped( "{}" ),
			// Force a password change on first sign-in: the seeded hash is public.
			// Auth.doLogin() detects a reset-pending account, issues a reset token and
			// sends the user straight into the reset-password flow, so no email round
			// trip is needed to complete the bootstrap.
			isPasswordReset: true,
			is2FactorAuth: false,
			verifiedAt: { value: now(), sqltype: "timestamp" }
		} )
		// Add the admin role to the admin user
		qb.newQuery().from( "user_roles" ).insert( {
			FK_userId : pgTyped( adminUserId ),
			FK_roleId : pgTyped( adminRoleId )
		} )

	}

}
