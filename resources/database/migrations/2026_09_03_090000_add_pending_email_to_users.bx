class {

    function up( schema, qb ) {
		schema.alter( "users", ( table ) => {
			table.addColumn( table.string( "pendingEmail" ).nullable().default( "" ) )
		} )
    }

    function down( schema, qb ) {
		schema.alter( "users", ( table ) => {
			// Pass the column definition (not just its name) so grammars that name
			// default constraints (e.g. SQL Server's "df_users_pendingEmail") know
			// to drop the constraint before the column instead of erroring that the
			// constraint still depends on it.
			table.dropColumn( table.string( "pendingEmail" ).nullable().default( "" ) )
		} )
    }

}
