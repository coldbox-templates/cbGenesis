class {

    function up( schema, qb ) {
		schema.create( "settings", (table)=>{
			table.string( "settingId", 36 ).primaryKey()
			table.boolean( "isActive" ).default( true )
			table.timestamps()

			// Setting specific fields
			table.string( "name" ).unique()
			// MySQL forbids a literal default on TEXT/BLOB/JSON columns; SettingService
			// always supplies a value on insert, so this is nullable instead of defaulted.
			table.longText( "value" ).nullable()

			// Indexes
			table.index( "isActive", "idx_settingActive" )
		} )
    }

    function down( schema, qb ) {
		schema.dropIfExists( "settings" )
    }

}
