class {

	function up( schema, qb ) {
		schema.alter( "users", ( table ) => {
			table.addColumn( table.boolean( "hasAvatar" ).default( false ) )
		} )
	}

	function down( schema, qb ) {
		schema.alter( "users", ( table ) => {
			// Pass the column definition (not just its name) so grammars that name
			// default constraints (e.g. SQL Server's "df_users_hasAvatar") know to
			// drop the constraint before the column instead of erroring that the
			// constraint still depends on it.
			table.dropColumn( table.boolean( "hasAvatar" ).default( false ) )
		} )
	}

}
